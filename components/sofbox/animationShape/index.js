import React from 'react';

const Index = (props) => (
    <div className={ props.animationShapeRight ? 'animationnew-shap' : 'animation-shap'}>
        <div className="shap-bg" />
    </div>
);

export default Index;